{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sma/schemas/agent-context-event.schema.json",
  "title": "SMA Agent Context Event",
  "description": "This schema defines one append-only event that records why an agent acted on a brick and what evidence it produced. Agent-tool authors, reviewers, and maintainers need it when they append or consume project context logs. Read it before emitting an event or reconstructing work after compaction. Remember to append a new event instead of rewriting prior history.",
  "x-sma-contract": {
    "version": "1.0.0",
    "change_policy": "Optional additive fields preserve v1 compatibility; required, renamed, or narrowed fields require a new versioned $id and schema_version.",
    "rollback": "Consumers retain the v1 decoder during the compatibility window; rollback drops only additive optional fields and never rewrites immutable or historical evidence.",
    "readers": [
      "tools/sma-validate-gen3.ts",
      "Gen3 controllers"
    ],
    "writers": [
      "tools/lib/context-log.ts",
      "tools/sma-context.ts"
    ],
    "lifecycle": "Created or regenerated by the listed writer, validated at its consumer boundary, and retained according to docs/SCHEMA_CONTRACTS.md.",
    "retention": "See docs/SCHEMA_CONTRACTS.md for the artifact-specific retention rule.",
    "performance": "Static file contract; database query plans, indexes, and full-scan budgets are not applicable. Validation performance is covered by schema-case selftests."
  },
  "type": "object",
  "required": [
    "schema_version",
    "event_id",
    "brick_id",
    "actor_kind",
    "actor_id",
    "kind",
    "intent",
    "timestamp"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "const": "1.0.0",
      "description": "Schema version that this document conforms to."
    },
    "event_id": {
      "type": "string",
      "description": "Stable id, format: ctx-<unix-ms>-<8 hex>."
    },
    "brick_id": {
      "type": "string",
      "description": "Stable identifier of the brick associated with this record."
    },
    "project": {
      "type": "string",
      "description": "Project identifier associated with the record."
    },
    "actor_kind": {
      "type": "string",
      "description": "Category of actor that performed the action.",
      "enum": [
        "human",
        "ai_model",
        "agent",
        "automation",
        "tool"
      ]
    },
    "actor_id": {
      "type": "string",
      "description": "Identifier of the human, agent, automation, or tool that performed the action."
    },
    "model": {
      "type": "string",
      "description": "Model identifier recorded for an AI-driven actor."
    },
    "session_id": {
      "type": "string",
      "description": "Identifier of the agent session that produced this value."
    },
    "task_id": {
      "type": "string",
      "description": "Identifier of the task associated with this value."
    },
    "lease_id": {
      "type": "string",
      "description": "Lease this event was performed under. Strongly encouraged."
    },
    "kind": {
      "type": "string",
      "description": "Classification assigned to the record.",
      "enum": [
        "lease_acquired",
        "lease_renewed",
        "lease_released",
        "lease_expired",
        "lease_force_acquired",
        "edit_planned",
        "edit_applied",
        "decision_recorded",
        "alternative_rejected",
        "verification_run",
        "proof_recorded",
        "promotion_attempted",
        "promotion_blocked",
        "release_cut",
        "merge_proposed",
        "merge_resolved",
        "conflict_detected",
        "conflict_resolved",
        "note"
      ]
    },
    "intent": {
      "type": "string",
      "minLength": 4,
      "description": "What the agent is doing or about to do. Plain sentence. Mandatory."
    },
    "decision_rationale": {
      "type": "string",
      "description": "Why this approach was chosen. Optional but expected on edit_applied and decision_recorded."
    },
    "rejected_alternatives": {
      "type": "array",
      "description": "Alternatives considered and rejected before this action.",
      "items": {
        "type": "object",
        "required": [
          "alternative",
          "reason"
        ],
        "additionalProperties": false,
        "properties": {
          "alternative": {
            "type": "string",
            "description": "An approach that was considered but not selected."
          },
          "reason": {
            "type": "string",
            "description": "Plain-language reason for this decision."
          }
        }
      }
    },
    "linked_backlog": {
      "type": "array",
      "description": "Backlog entry identifiers connected to this record.",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "files_touched": {
      "type": "array",
      "description": "Repository-relative files changed by the recorded work.",
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "commit": {
      "type": "string",
      "description": "Git commit identifier associated with the recorded work."
    },
    "verification": {
      "type": "object",
      "description": "Fields that describe the verification.",
      "additionalProperties": false,
      "properties": {
        "command": {
          "type": "string",
          "description": "Command used to produce or verify this evidence."
        },
        "status": {
          "enum": [
            "pass",
            "fail",
            "skipped",
            "blocked"
          ],
          "description": "Current status of the verification."
        },
        "output_path": {
          "type": "string",
          "description": "Path to the stored command output or evidence artifact."
        },
        "notes": {
          "type": "string",
          "description": "Additional notes about the verification."
        }
      }
    },
    "proof": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "uniqueItems": true,
      "description": "Concrete proof commands or artifact references produced for this brick."
    },
    "gain_percent": {
      "type": "number",
      "description": "Optional measured or estimated improvement captured with proof evidence."
    },
    "timestamp": {
      "type": "string",
      "description": "Time when the event occurred, in ISO 8601 date-time format.",
      "format": "date-time"
    }
  }
}
